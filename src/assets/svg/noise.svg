<?xml version="1.0" encoding="utf-8"?>

<svg xmlns="http://www.w3.org/2000/svg" id="filter_group" data-name="Layer 1" viewBox="0 0 570.9 139.6">
  <defs>
    <filter id="squiggly-0">
      <feTurbulence id="turbulence" baseFrequency="0.02" numOctaves="2" result="noise" seed="0"/>
      <feDisplacementMap in="SourceGraphic" in2="noise" scale="2"/>
    </filter>
    <filter id="squiggly-1">
      <feTurbulence id="turbulence" baseFrequency="0.02" numOctaves="4" result="noise" seed="1"/>
      <feDisplacementMap in="SourceGraphic" in2="noise" scale="3"/>
    </filter>
    <filter id="squiggly-2">
      <feTurbulence id="turbulence" baseFrequency="0.02" numOctaves="2" result="noise" seed="5"/>
      <feDisplacementMap in="SourceGraphic" in2="noise" scale="4"/>
    </filter>
    <filter id="squiggly-3">
      <feTurbulence id="turbulence" baseFrequency="0.02" numOctaves="3" result="noise" seed="3"/>
      <feDisplacementMap in="SourceGraphic" in2="noise" scale="3"/>
    </filter>
    <filter id="squiggly-4">
      <feTurbulence id="turbulence" baseFrequency="0.02" numOctaves="50" result="noise" seed="10"/>
      <feDisplacementMap in="SourceGraphic" in1="noise" in2="noise" scale="10"/>
    </filter>
    <filter id="squiggly-5">
      <feTurbulence id="turbulence" baseFrequency="0.02" numOctaves="20" result="noise" seed="10"/>
      <feDisplacementMap in="SourceGraphic" in1="noise" in2="noise" scale="5"/>
    </filter>
    <filter id="squiggly-6">
      <feTurbulence id="turbulence" baseFrequency="0.02" numOctaves="50" result="noise" seed="10"/>
      <feDisplacementMap in="SourceGraphic" in1="noise" in2="noise" scale="10"/>
    </filter>
  </defs>
</svg>
